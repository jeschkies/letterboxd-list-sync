[package]
name = "letterboxd-list-sync"
version = "0.1.0"
authors = ["karsten", "boxdot <d@zerovolt.org>"]
edition = "2018"

[dependencies]
docopt = "0.8"
futures = "0.1"
letterboxd = "0.1"
regex = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio-core = "0.1"
