[package]
name = "letterboxd-list-sync"
version = "0.1.0"
authors = ["karsten"]

[dependencies]
docopt = "0.8"
letterboxd = { git = "https://github.com/boxdot/letterboxd-rs.git" }
serde = "1.0"
serde_derive = "1.0"
